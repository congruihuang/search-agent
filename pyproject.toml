[project]
name = "search-agent"
version = "0.1.0"
description = "Autonomous web-research agent using tree-structured context management"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.52.0",
    "mcp>=1.0.0",
]

[project.scripts]
search-agent = "src.search_agent:main"
